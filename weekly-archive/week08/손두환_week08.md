# Chapter4 유용한 선형회귀
## 선형회귀에서의 비선형성
**양수성과 외삽**
- 양수성(positivity, common support): 모든 $x$에 대해 $0< P(T=1\mid X=x) <1$이어야 조건부효과 식별이 가능하다.
- 특정 $X$ 영역에서 한 처리군만 관찰되면 외삽이 불가피하고 추정은 모델가정에 민감해진다. 즉 오차가 매우 커질 수 있다.
- (외삽 위험을 줄이기 위해 가중법(IPW), 이중강건추정(DR), 지역적추정(지역화)을 고려할 수 있다고 함)

**선형회귀에서의 비선형성**  
처치 선형화
- 반응함수 $E[Y\mid T,X]$가 비선형일 때 단순 선형회귀로는 안된다.
  - 비선형인지 테스트 하는법 : Ramsey RESET test 등이 있다. 
    - 기존 선형회귀 fitting
    - x^2, x^3등등 term 추가해서 fitting 
    - 귀무가설: 추가된 term 계수가 0, 대립가설: 추가된 계수가 0이 아님 
    - Box–Tidwell test도 있음. 그러나 제한된 비선형성만 검정하는거 같아 완벽하지는 않은듯.
- log, sqrt 등 적용하여 비선형 fitting 해야함
- 반응함수 $E[Y\mid T,X]$가 비선형일 때 국소선형 근사로서 회귀계수는 평균국소효과를 제공한다. 부분선형모형(semiparametric partial linear model)
$$
Y = T\beta + g(X) + \varepsilon
$$
은 $g(\cdot)$를 비모수적으로 처리하면서 $\beta$를 일관되게 추정하는 대표적 틀이다(FWL 이용 가능).

### 비선형 FWL과 편향제거
- 처치 선형화 단계
    - 목표: 반응함수 $E[Y│T,X]$가 비선형일 때 T와 Y의 관계를 선형화할 수 있는 변환 $F(\cdot)$ 또는 국소근사(예: 다항식, 스플라인, basis expansion)를 선택한다. 즉
      $$
      E[Y\mid T,X]\approx \alpha + \beta\,F(T) + g(X).
      $$
    - 실무적 선택: F는 단순한 항등(identity), 로그, 다항식, 또는 스플라인·로컬선형 근사 등이 될 수 있다. 관심이 평균변화율이라면 $F(T)=T$(또는 미분·차분으로 근사)로 잡는다.
- 편향 제거 (treatment residualization)
    - F(T)를 공변량 X로 회귀하여 처치의 잔차(부분화된 처치)를 얻는다:
      $$
      \hat m_F(X)\approx E[F(T)\mid X],\qquad \tilde F_i = F(T_i)-\hat m_F(X_i).
      $$
    - 이 잔차는 X로부터 부분화된 처치 구성요소로, 교란요인에 의한 편향을 제거하는 역할을 한다.
    - 이 단계에서의 추정은 비선형성에도 불구하고 1단계에서 충분히 정확히(속도조건 만족) 추정되면 최종 추정의 편향을 제거한다.

- 잡음 제거 (outcome residualization)
    - 결과 Y를 X에 대해 회귀하여 결과의 잔차를 얻는다:
      $$
      \hat g(X)\approx E[Y\mid X],\qquad \tilde Y_i = Y_i-\hat g(X_i).
      $$
    - 결과 잔차는 X로 설명되는 부분을 제거하여 처치와의 순수한 관계를 드러낸다.
- 결과 모델 단계 (second‑stage estimation)
    - 2단계에서 $\tilde Y$를 $\tilde F$에 대해 단순회귀하면 F(T)의 Y에 대한 인과효과 추정치를 얻는다:
      $$
      \hat\beta=\frac{\sum_i \tilde F_i\tilde Y_i}{\sum_i \tilde F_i^2}.
      $$
    - 이 추정량은 double/debiased 형태의 아이디어를 따르므로, 1단계 추정오차가 충분히 작으면 표본크기 기준으로 일관적이고 근사정규분포를 따른다(유효성 확보를 위해 교차적합 및 속도조건 필요).
    - 표준오차·신뢰구간: 2단계 회귀의 표준오차를 사용하되, 1단계 추정불확실성 제거를 위해 교차적합된 표본구조에 맞는 표준오차 계산(또는 influence‑function 기반 표준오차)을 권장한다.

- 추가 실무 권고
    - F 선택의 민감도 점검: 여러 F(예: 원시·로그·스플라인)로 반복하여 결과의 견고성 확인.
    - 지지(common support) 및 외삽 위험 검토: X의 일부 구간에서 처치 분포가 좁으면 외삽에 민감함.
    - 해석 주의: 추정된 β는 선택한 F에 대한 평균(또는 국소) 효과임. 예를 들어 F가 스플라인이면 β는 그 basis에 대한 계수로서 직접적 해석은 변환에 의존한다.
    - 이론적 근거: 위 절차는 FWL(Frisch–Waugh–Lovell) 아이디어의 비선형 확장으로 볼 수 있으며, 머신러닝을 1단계에 사용하더라도 교차적합과 속도조건을 만족하면 일관성과 정규근사를 얻는 근거가 있다.

## 더미변수를 활용한 회귀분석
회귀분석과 직교화는 훌륭하지만, 독립성 가정을 해야했음: 일부 공변량을 통제했을 때 처치가 무작위로 배정된 것처럼 보이도록 가정해야함.  
가능하면 무작위 실험하는편이 좋지만, 무작위 통제 실험은 큰 비용이 듦. (채무불이행 가능성인 높은 사람한테 무작위로 신용한도 줘야하는 꺼림칙한 짓을 해야함)  
이를 해결하기 위해, stratified (계층화) 또는 conditionally 무작위 실험이 제안됨! (이 전 방법은 완전 무작위 실험이라고 부름)  
공변량에 따라 서로 다른 분포에서 표본을 뽑아 여러 국소 실험을 만듬.  

### 조건부 무작위 실험
- 완전 무작위는 아니고, 적당한 분포로 T처리하기 전 샘플을 뽑되, 모든 X를 span하기는 하게 함.
  1. T처리하기 전 샘플들을 even하게 뽑지 않고, 약간 외곡을 준다.
  2. 각 신용점수 별로 그룹핑, 분포를 계산해서 그 빈도대로 후보들 세팅
  3. 예를들어 50%확률로 T처리해서 각 bin k별로 ATE계산한 다음 가중평균
  - 신용한도 낮은사람한테 채무이행률을 테스트하는 부담을 줄이는 목적
  - ATE계산 시 직접 group개념이 쓰이는게 아니고, T 처리 전 샘플링에만 사용된다.
- 그룹 수가 적을수록 덜 복잡해진다.
- 그룹 간 처치분포가 곂칠수록 좋다. 양수성 가정이 지켜지니까. 양수성 가정이 깨지면 위험한 외삽(parametric model로 샘플링)에 의존해야했었음.
  - 하지만, 실제 샘플이 없다면 결국 양수성 가정 깨짐
  - 해결법: 
    - 더 넓은 bin으로 통합 (coarsening)
    - 모델 기반 추정 (parametric extrapolation)
    - 성향점수 매칭(IPW, matching)
- 물론, 그룹수가 적고, 곂치는 부분이 많을수록 큰 비용이 들어가서 적당한 trade off를 해야함.

더미변수
- 조건부 무작위 실험의 장점: 설정한 범주형 변수에 따라 신용한도가 무작위로 배정되었음을 아니까, 조건부 독립 가정에 설득력이 생김
- 실험군에 대한 결과만으로 단순 회귀분석을 하면 편향된 추정값을 얻는 단점이 있음.
  - 이를 보정하기위해 모델 처치가 무작위로 배정된 그룹 정보를 포함해야함
  - 그룹별 one-hot encoding 변수를 추가해주고 선형회귀식에 추가
- 더미의 해석: 기준(base) 대비 효과. 다중더미 시 완전상관(multicollinearity)에 주의(더미트랩).

### (추가) interaction
상호작용(interaction)은 두 변수의 곱을 회귀식에 넣어 효과가 그룹마다 달라지도록 허용하는 항이다. 이진 처치 $T\in\{0,1\}$와 배타적 범주 더미 집합 $G_1,\dots,G_K$에 대해 상호작용을 포함하면 각 그룹의 그룹별 인과효과(CATE)를 직접 추정할 수 있다.

1. 상호작용 항의 정의  
상호작용항은 $T\times G_k$로 표기되며, “그룹 $k$에서 처치의 기울기(효과)가 달라질 수 있음”을 의미한다.

2. 포화(saturated) 규격  
- (A) 기준범주(reference) 포함형: 절편과 기준범주 $G_1$을 두고 나머지 $K-1$개 더미만 포함하는 경우 $$Y=\alpha+\tau T+\sum_{k=2}^K\gamma_k G_k+\sum_{k=2}^K\delta_k (T\times G_k)+e.$$
    - 해석:
      - $E[Y\mid T=0,G_1]=\alpha$ (기준범주 대조군 평균)  
      - $E[Y\mid T=1,G_1]=\alpha+\tau$ → $G_1$의 처치효과 $=\tau$  
      - $E[Y\mid T=0,G_k]=\alpha+\gamma_k$  
      - $E[Y\mid T=1,G_k]=\alpha+\gamma_k+\tau+\delta_k$ → $G_k$의 처치효과 $=\tau+\delta_k$  
      - 따라서 $\delta_k\neq0$이면 그룹 $k$의 효과가 기준범주와 다르다.

- (B) 무절편(cell-mean) 포화형: 절편을 빼고 모든 더미와 모든 상호작용을 포함하는 경우 $$Y=\sum_{k=1}^K\beta_k G_k+\sum_{k=1}^K\theta_k (T\times G_k)+e.$$ 
    - 해석:
      - $E[Y\mid T=0,G_k]=\beta_k$  
      - $E[Y\mid T=1,G_k]=\beta_k+\theta_k$ → 그룹 $k$의 처치효과 $=\theta_k$  

- 두 사양(A,B)은 재파라미터화 관계이며, 그룹별 CATE 추정값은 동일하다.

3. “평균 비교와 동일” 성질  
FWL 정리에 따라 각 $k$에 대해
$$
\widehat{\mathrm{CATE}}_k=\widehat{E}[Y\mid T=1,G_k]-\widehat{E}[Y\mid T=0,G_k]
$$
이고, 이는 OLS로 얻는 계수($\tau$, $\tau+\delta_k$ 또는 $\theta_k$)와 동일하다. 즉, 상호작용을 포함한 회귀는 그룹별 평균차이를 한 번에 추정하는 것과 동치이다.

4. 인과적 해석의 조건  
그룹별 효과를 인과적으로 해석하려면 최소한 다음이 필요하다.
- SUTVA (Stable Unit of Treatment Value Assumption: 한 실험대상의 처치가 다른 대상의 결과에 영향을 미치면 안됨).  
- 비혼합성(ignorability): $(Y_1,Y_0)\perp T\mid G$ 또는 더 넓게 $(Y_1,Y_0)\perp T\mid X$ 및 $G=g(X)$.  
- 양의 확률(positivity): 모든 $k$에 대해 $0<P(T=1\mid G_k)<1$.

이 조건들이 충족되면 $\theta_k$ (또는 $\tau+\delta_k$)는 그룹 $k$의 CATE에 일치한다.

5. 전체 ATE와의 관계  
그룹 비율 $\pi_k=P(G_k)$로 가중하면 전체 ATE는
$$
ATE=\sum_{k=1}^K \pi_k\cdot \mathrm{CATE}_k.
$$

6. 실무 팁
- 더미 트랩(dummy trap) 방지: 절편을 둘 때는 한 범주를 기준으로 제외하거나, 절편을 빼고 모든 더미를 포함할 것.  
- 표준오차: 그룹별 표본 크기가 작거나 이분산이 의심되면 robust(HC) 표준오차 사용 권장.  
- 다중비교: 많은 그룹에서 검정을 반복하면 유의성 오탐이 증가하므로 사전 지정된 하위그룹이나 다중비교 보정 절차를 고려할 것.  
- 해석 주의: 추정된 그룹별 계수는 해당 그룹의 조건부 평균효과이므로 식별 가정을 명확히 보고할 것.
- 표본편중 또는 지지 부족(common support)이 있는 그룹에서는 외삽에 민감하므로 결과의 견고성(예: 지지절단, 서브샘플 분석)을 점검할 것.
- 상호작용을 포함한 모형은 그룹별 이질성을 드러내지만, 표본 크기 및 다중비교 문제를 함께 고려해야 한다.

### 포화회귀모델
- 포화모델: 회귀분석에서 모든 가능한 설명변수와 그 상호작용을 포함하는 모델, 과적합 위험이 있고 일반화 능력이 떨어질 수 있으나, 특정 그룹의 인과효과 파악에 유용함
- 범주별 관찰치가 충분하면 포화모형은 가장 유연한 회귀이며, 실질적으로 그룹간 평균비교와 동일하다.
- 이 책에서는 첫번째 그룹을 생략하고 상호작용 매개변수를 넣음.
- 상호작용 매개변수 해석은 생략된 첫 번째 그룹의 효과와 연관지어 해석해야함
- T와 관련된 매개변수와 상호작용 항을 더하면, 그룹별로 추정된 효과를 얻는다. 
  - 이는 그룹별로 하나의 효과를 각각 추정할 때와 결과가 같다 (한번에 처리한 느낌)
  - 그룹별로 절편, 기울기가 다 다름
- 포화모델은 매개변수(자유도)가 더 많고, 이는 다른 모든 조건이 동일할 때 분산이 더 큼을 의미
  - 작은 표본크기로 인한 잡음으로 통계적으로 유의미하지 않은 기울기가 나올수도 있음

**분산의 가중평균과 회귀분석**
- 회귀분석이 그룹효과를 결합할 때, 표본크기를 가중치로 사용하지 않음
- 각 그룹에서 처치의 분산에 비례하는 가중치를 사용함.
- 처치가 많이 변할수록, 결과에 미치는 영향이 더 분명하므로 처치 변동성이 큰 그룹을 회귀분석은 선호함
- 즉, 여러 그룹이 있고 각 그룹 내에서 처치가 무작위 배정된 경우, 조건부 원칙(보정공식)에 따르면 효과는 각 그룹내 효과의 가중평균임
$$ ATE = E\{(\frac{\partial}{\partial t}E[Y_i \mid T=t, Group_i])w(Group_i)\}$$
- $w(Group_i)$를 각 그룹의 처치의 분산으로 쓰기도 하고, 그룹크기(샘플수)로 하기도 함  

**평균제거와 고정효과**
- FWL은 더미변수가 있는 상황에서 더 유용하다
- 평균제거(demeaning)로 처리 가능하며, 이는 FWL의 특수한 
적용이다:
  - 4부: 시계열(패널) 데이터를 사용한 인과추론에서 다시 볼 것임
$$
Y_{it}​ =  \alpha_i ​+ X_{it}\beta + \epsilon_{it}​
$$
시간에 따른 식이 있고, $alpha_i$는 고정임. 따라서 시간에 따른 (그룹에 따른) 평균을 취해서 식 하나를 처리하겠다는 뜻인듯.
$$
Y_{it}-\bar Y_i = (X_{it}-\bar X_i)\beta + (\varepsilon_{it}-\bar\varepsilon_i).
$$
- 이 방식으로 얻은 매개변수 추정값은 모델에 더미변수를 추가해서 얻은 값과 동일함
  - 수학적으로 동등하다고 증명된듯?
- 이렇게 그룹 내에서 변하지 않는 고정된 모든 요인을 통제한 모델을 fixed effect model이라 함
## 누락 변수 편향 (Omitted Variable Bias, OVB)
- 진짜 모형:  
    $Y = \tau T + \beta_{\text{omitted}}' Z + u$
- 연구자가 $Z$를 관측하지 못하고 단순회귀만 하면:  
    $Y = \hat\tau T + e$
- 이때 $\hat\tau$에는 $Z$의 영향이 섞여 다음과 같이 편향이 발생한다. $$\frac{\operatorname{Cov}(T,Y)}{\operatorname{Var}(T)}=\tau+\beta_{\text{omitted}}'\,\delta_{\text{omitted}}$$
  - $\beta_{\text{omitted}}'$: 결과 $Y$에 대한 누락변수 $Z$의 회귀계수 벡터(즉 $Y$에 대한 $Z$의 영향).
  - $\delta_{\text{omitted}}$: 처치 $T$를 $Z$로 회귀했을 때의 계수 벡터(즉 $Z$가 $T$에 미치는 영향).

따라서 누락변수 $Z$가 결과와 처치 모두와 관련되어 있으면, 편향은 이 두 계수의 곱만큼 발생한다.

- OLS 단순회귀의 계수 = (진짜 인과효과) + (누락변수로 인한 편향항).  
  - 즉, 누락변수가 결과와 처치 모두와 관련되면 편향이 생긴다.

**해결 전략(실무적)**
- 추가 통제: 가능한 한 $Z$를 측정하여 회귀에 포함.  
- 도구변수(IV): $T$와 상관되지만 $Z$와 무관한 도구 사용.  
- 패널 고정효과(FE): 시간불변 누락요인 제거.  
- 차분(DID): 동일 개체의 전·후 차이로 불변요인 제거.  
- 무작위화: $T$를 무작위 배정하여 $\operatorname{Cov}(T,Z)=0$ 확보.
- (각 방법은 가정과 한계가 있으므로 적용 전 가정 검토 및 민감도 분석 권장.)

## 중립 통제변수
- 교란 요인을 보정하기 위해 X를 추가했었음.
- 어떤 X를 추가해야할까? 충동부/매개자는 선택편향을 유발하므로 피해야함.
- 어떤 변수들은 편향을 유발하지는 않음
  - 이런 변수들을 neutral하다고 함
  - neutal하지만 분산에는 영향을 줄 수 있다고 함
  - 회귀모형에 특정 변수를 넣으면 bias-variance tradeoff가 존재함
- "중립 통제"란 편향을 제거하지는 않지만 분산을 줄여 효율성을 높이는 변수들(예: 결과에만 영향, 처리에는 영향이 없는 변수)
- 실제로는 처리와 상관관계가 없는 통제변수는 추정의 일관성에 영향을 주지 않지만 분산추정에는 영향이 있다.
- (참고: 다른 잡음 제거 기법도 있는데, Microsoft에서 쓰는 CUPED기법이 있다고 함)

**특성 선택: bias–variance tradeoff**
- 더 많은 변수를 넣으면 편향은 줄지만 분산은 증가할 수 있고(특히 소표본), 반대의 경우도 성립한다. 
- 그래서 하나의 교란요인이 처치의 강력한 predictor이고, 결과에는 적은 영향만 준다는걸 알면, 이 요인을 빼기도 함
- 변수선택 기법 (ChatGPT정리 내용. 검토 필요):
    - 규제(regularization): 릿지(Ridge), 라쏘(LASSO) — 예측성·선형조합 선택에 유리.
    - 교차검증(cross‑validation): 일반화오차 최소화.
    - 이중절차(double selection): 변수선택과정에서 처리와 결과 모두를 고려해 선택하면 인과추정의 편향을 줄이는 실전적 방법.
- 최근 방법론: Double/Debiased Machine Learning(교차적합 + orthogonalization)으로 고차원 설명변수 상황에서도 일관적이고 근사정규적 결과를 얻을 수 있다.

# Chapter5 성향점수
4장의 직교화 말고 또 다른 편향제거 방법인 propensity weighting (성향점수 가중치)를 다룸.  
처치 배정 매커니즘을 모델링하고, 모델 예측을 사용하여 데이터를 weighting함  
double robustness: 4장의 직교화 + 성향점수 가중치  

## Propensity Weighting
**balancing score**
- 처치의 조건부 확률, $P(T \mid X)$나 성향점수($e(x)$)로 표현
- X를 T로 변환하는 일종의 함수 (?)
- 교란요인 X를 직접 통제할 필요 없이 CIA를 만족할 수 있다는 깨달음에서 비롯함
- 교란요인을 통제하는 대신, $E[T \mid X]$를 추정하는 balancing score를 통제해도 충분!  
- e(X)를 알면, X만으로는 T에 관한 추가정보를 얻을 수 없다. 즉 X를 직접 통제할 때와 동일한 효과가 나옴 (X to Y를 통제할수는 없긴하지만, 나중에 나오지만 이 방법들은 샘플 분포 자체를 바꿔서 추론하는 것이므로 효과는 있는듯)
$$ 
\begin{array}{ccccccccc}
 & & X & & \\
 & \swarrow & & \searrow & \\
 e(X) & \rightarrow & T & \rightarrow & Y
\end{array}
$$

- Balancing Score Theorem: 고차원 공변량 $X$ 전체를 조건부로 두지 않고,
  성향점수 $p(X) = P(T=1\mid X)$ 만을 조건부로 두어도
  $$(Y_1, Y_0) \perp T \mid p(X)$$
  가 성립한다. (차원축소로 해석 가능)
  - 증명  
      여기서 $P(\cdot)$는 확률연산자, $E[\cdot]$는 기대값, 성향점수는 $p(X) := P(T=1\mid X)$ 로 둔다. 또한 CIA: $(Y_0, Y_1)\perp T \mid X$ 성립함 가정.

    $$
    \begin{aligned}
    P(T=1 \mid Y_j,\, p(X))
    &= E[T \mid Y_j,\, p(X)] \\[4pt]
    &= E\!\left[\,E[T \mid Y_j,\, p(X),\, X] \mid Y_j,\, p(X)\,\right] 
    \quad (\text{iterated expectation}) \\[4pt]
    &= E\!\left[\,E[T \mid X] \mid Y_j,\, p(X)\,\right]
    \quad (\text{CIA: } (Y_0,Y_1)\!\perp\!T \mid X \text{, p(X) can be expressed by X}) \\[4pt]
    &= E\!\left[\,p(X) \mid Y_j,\, p(X)\,\right]
    \quad (\text{definition: } p(X)=P(T=1\mid X)) \\[4pt]
    &= p(X)
    \quad (\text{conditional expectation rule: } E[Z\mid Z,W]=Z)
    \end{aligned}
    $$
     
    > [참고: sufficient statistic]  
    > 성향점수는 $p(X) := P(T=1\mid X)$ 로 정의된다. 이때
    > $$
    > \begin{aligned}
    > P(T=1\mid p(X))
    > &= \mathbb{E}\big[\,P(T=1\mid X)\mid p(X)\,\big]  \\
    > &= \mathbb{E}\big[\,p(X)\mid p(X)\,\big]  \\
    > &= p(X).
    > \end{aligned}
    > $$
    > (조건부기대값 성질 $E[Z\mid Z,W]=Z$ 사용.)  
    > 특히 $T\in\{0,1\}$ 이진 처치이므로,
    > 모든 $t\in\{0,1\}$ 에 대해
    > $$P(T=t\mid X) = P(T=t\mid p(X))$$
    > 가 성립한다.  
    > 따라서 $p(X)$는 $T$에 대한 $X$의 정보를 완전히 요약하는 
    > 충분통계(sufficient statistic)이자 balancing score이며,
    > 조건부로 보면
    > $$T \perp X \mid p(X)$$
    > 이다.

    따라서
    $$P(T=1 \mid Y_j, p(X)) = p(X) = P(T=1 \mid p(X)),$$
    즉 조건부 분포가 $Y_j$에 의존하지 않으므로
    $$T \perp Y_j \mid p(X)$$
  가 성립한다.
    
- e(x)를 실제로 알수는 없고, 추정해야함
  - 머신러닝으로 해도되지만
    - calibrated probability가 나와야하고
    - overfitting에 의한 bias를 피하기위한 out of fold 방식의 예측이 필요함

**Propensity Score Matching (PSM, 성향점수 매칭)**  
- matching estimator: 성향점수를 통제하는 다른 방법  
- 관측가능한 특징이 비슷한 실험대상의 짝을찾아서 실험군(T=1), 대조군(T=0)를 비교함  
- KNN같은 매칭알고리즘 사용하여 counterfactual data를 생성함  
$$\hat{ATE} = \frac{1}{N}\sum{\{(Y_i-Y_{jm}(i))T_i+(Y_{jm}(i)-Y_i)(1-T_i) \}}$$
- 잘 알려져있는 방법이긴 하지만, 편향될 가능성이 있고, 분산을 추정하기 어렵고, KNN이 고차원이면 느려서 별로라고 함.  
- TODO:FIXME: 이게 구체적으로 어떤건지, 

## Inverse Propensity Weighting (IPW, 역확률 가중치)
- PSM이외에도 성향점수를 활용하는 일반적인 방법.  
- 처치의 inverse probability에 따라서 데이터의 가중치를 재조정하여 처치가 무작위 배정된 것처럼 보이게 함 
- $1/P(T=t|X)$ 가중치를 표본에 부여하여 모든 실험대상이 T=t를 받았을 경우와 비슷한 pseuso-population을 생성함
$$E[Y_i]=E[\frac{\bold{1}(T=t)Y}{P(T=t \mid X)}]$$

$$w_i​=\frac{T_i}{\hat{P}(T_i​=1∣X_i​)}​​+\frac{1−T_i}{1−\hat{P}(T_i​=1∣X_i​)}$$
- 표본 전체를 평균 낼 때 각 집단별 확률 가중치를 붙이면
모집단 평균과 같아진다

- 실험군에 속한 대상이 처치 받을 확률이 낮다면, 해당 대상은 대조군과 매우 비슷함
  - $Y_1|T=0$에 관한 매우 유용한 정보를 제공할 수 있으므로 높은 가중치를 부여함
- 대조군에 속한 대상이 실험군과 매우 유사하다면, $Y_0|T=1$의 좋은 추정값이 될 수 있으므로 더 많은 가중치를 부여함
$$ATE = E[\frac{\bold{1}(T=1)Y}{P(T=1 \mid X)}]-E[\frac{\bold{1}(T=0)Y}{P(T=0 \mid X)}]$$
- 역확률 가중치로 얻은 결과는 OLS결과와 매우 유사해서, 디버깅하는데 좋다
- 아래 식처럼 단순화 할수도 있음
$$ATE = E[Y\frac{T-e(x)}{e(x)(1-e(x))}]$$

**IPW의 분산**  
- IPW의 표준오차 계산은 OLS보단 복잡함: Bootstrap 등의 방법을 쓴다 (데이터 복원추출해서 추정값 구함)  
- 큰 가중치가 적용되면 추정량의 분산이 크게 증가함. 최종 추정값에 큰 영향을 주는 소수의 대상이 분산 증가의 원인이 된다.  
- 가중치 안정화 방법: 처치의 marginal probability P(T=t)를 사용
  - $w = \frac{P(T=t)}{P(T=t|X)}$
  - 가중치 전체를 P(T=t)로 줄여서, $\sum{w} = N$이 되게한다. 
  - P(T=t)적용 안하면 N보다 훨씬 커져 분산이 매우 커질 수 있음

**선택 편향**  
- IPW는 선택편향 보정에도 사용될 수 있다. 
- 이를 호르비츠-톰슨 추정량이라 함 (Horvitz-Thompson estimator)  
- ex: 고객만족도 설문조사에서, 미응답자 대부분이 앱에 불만족한 고객이라면, 설문결과는 selection bias로 좋은쪽으로 부풀려질 수 있다.  
  - 이에 고객의 공변량으로 응답률(R)을 추정할 수 있다.  
  - 그다음 응답자에게 $1/\hat{P}(R=1)$만큼 가중치 부여  
  - $\hat{P}(R=1)$이 낮다는것은 미응답자와 유사하다는 것. 이들에 높은 가중치를 줌.
  - 이게 모두가 설문에 응답한 것 처럼 보이는 유사 모집단을 생성함
  - 즉, 원본 분포를 바로 쓰는게 아니라, 이런 숨은 집단의 분포를 반영한 보정 분포로 추론하는게 정확하다는 의미.

**bias-variance trade off**  
이런 테크닉으로 인과추론 문제를 예측문제로 바꿀 수 있어보이지만, variance가 늘어남.  
P(T/X)값 추정을 완벽하게 잘해버리면, 대조군이 남지않게되고, 처치효과 추정값이 이상하게됨(variance가 늘어남)
양수성-unconfoundedness trade-off라고도 함.  
성향점수가 낮은(대조군이 될 확률이 높은) 영역에 표본이 없다면, 양수성 가정 위반으로 재조정은 불가능함.  
양수성이 완전히 위배되지 않아도, 일부 실험대상의 성향점수가 매우 작거나 크면 IPW는 큰 분산을 겪는다.

회귀분석은 실제 데이터가 없는 영역까지 외삽하지만(parametric 가정), IPW는 잠재적 결과의 형태에 대해 아무런 가정도 하지 않아서 ATE추정이 어려울 수 있다.  

질문: e fitting 기준은 머신러닝 모델 rmse줄이는거 같고, 이를 통해 IPW로 새로 샘플링을 하고 인과추론 통계량을 선형회귀로 구함. 하지만 e fitting 단계에서 인과추론양 까지 고려할 수 있을까? 
하지만, 문제가 생긴다!
  1. 데이터 스누핑(data snooping) / post-treatment bias
    - 결과 Y를 보면서 e(X)나 샘플링 방법을 조정하면, 결과에 맞춘 설계(result-driven design)이 되어 편향이 생긴다.
    - 특히 ML 기반 모델에서 과적합과 유사하게, 실험 설계가 Y의 분포에 맞춰지므로 causal validity가 붕괴된다.
  2. 표준오차 과소추정
    - 설계와 분석이 한꺼번에 수행되면, 불확실성이 하나의 단계로만 계산되어 과도하게 낙관적인 신뢰구간이 만들어진다.

## design-based vs model-based identification
회귀분석, IPW 중 언제 어떤 방법을 사용하는게 좋은가?  
처치가 어떻게 배정되었는가? 잠재적 결과 모델을 올바르게 지정하는 더 좋은 방법이 있나? 이 두가지 질문으로 아래 어떤 방법이 나은지 판단하자.  
1. 모델기반 식별
  - 목표: 추정에 필요한 누락된 결과 대체
  - 처치, 추가 공변량을 조건부로 설정하고, 잠재적 결과에 대한 모델 형태로 가정
  - 회귀분석 잠재적 결과 모델 추정량 구하기
2. 디자인 기반 식별
  - 이번 챕터 접근법
  - 처치 배정 매커니즘에 대한 가정
  - 회귀분석 직교화

둘 중에 어떤게 나은지 확신이 없으면 두가지 모두 사용해도 된다: 아래 doubly robust 추정
## doubly robust (DR) 추정
모델기반, 디자인기반 식별 모두 결합하여 둘 중 하나가 정확하기를 기대함.  
둘 중 하나만 정확하게 지정해도 효과가 있음  
반사실 $Y_t$의 이중 강건 추정량:  
$$
\hat{\mu}^{DR}_t(\hat{m}, \hat{e})
= \frac{1}{N}\sum_{i=1}^{N} \hat{m}(X_i)
+ \frac{1}{N}\sum_{i=1}^{N} \frac{T_i}{\hat{e}(X_i)}(Y_i - \hat{m}(X_i))
$$

여기서  
- $\hat{m}(X)$ : 결과모델 (예: $\mathbb{E}[Y|X]$, 선형회귀로 추정)  
- $\hat{e}(X)$ : 성향점수모델 (propensity score, $P(T=1|X)$)
- $\hat{\mu}^{DR}_t(\hat{m}, \hat{e})$: 평균 반사실적 결과인 $Y_t$ 추정

결과모델은 정확하고, 성향점수모델이 이상한 경우: 좌항만 남고 우항은 0에 수렴  
결과모델 이상하고, 성향점수 모델이 정확한 경우: $\frac{1}{N}\sum \frac{TY}{\hat{e}(X)}$만 남고, 이는 IPW추정량으로 성향점수 모델이 정확하므로 이 값도 정확함.  

TODO:FIXME: 얘는 뭔데 이런 식이 타당한건가? 
둘 다 이상하면 어쩔수 없겠지? X가 이상해서 T, Y 추정 둘다 어려운 경우?  


이를 활용해 ATE구하기:
$$
\widehat{ATE}
= \hat{\mu}^{DR}_1(\hat{m}, \hat{e})
- \hat{\mu}^{DR}_0(\hat{m}, \hat{e})
$$

(참고: DR말고 다른 추정량도 있음 ([Robins J. et al, 2007](https://arxiv.org/pdf/0804.2965])))

## 연속형 처치에서의 일반화 성향 점수
continuous treatment를 다루지 않은 이유는 훨씬 복잡하고, 아직 연구중인 과제임!  
처치반응의 함수형태를 알면 4장처럼 해결할수는 있음.  
혹은 연속형 처치를 discretization하거나  
generalized propensity score (GPS)사용 가능  

문제1: 연속형 변수가 어디서나 확률이 0  
=> 해결 단계: 
1. 확률 P(T=t|X) 대신 조건부 밀도함수 f(T|X) 사용. 하지만 f를 정해야함
 - 보통 정규분포 사용함
 - 다른 분포: generalized linear model (GLM) 사용해서 일반화 성향점수 가중치 만듦
 - 처치를 예측하는 모델로 처치효과를 추정하는 다른 방법들
    1. 회귀분석에 일반화 성향점수를 공변량으로 포함
    2. (1이랑 다른 방법) T를 적합하고 $\hat{T}$로 데이터를 세분화하고, 각 세분화된 그룹에서 처치를 결과에 회귀, 각 그룹의 크기를 가중치로 가중평균하여 결과 합치기
2. 함수f 매개변수인 평균, 표준편차 추정
 - OLS로 처치변수를 적합해서 적합된 값을 평균, 잔차의 표준편차를 표준편차로 사용

3. 이 함수에 연속형 변수 T를 넣어서 GPS값 추출
 - 이 값의 역수를 가중치로 사용하여 편향 보정.. 하면되지만, 가중치 안정화 안하면 결과가 좀 틀림
 - 연속형 처치에서 가중치 안정화는 필수!!!
